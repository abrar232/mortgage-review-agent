apiVersion: v1
kind: Service
metadata:
  name: mortgage-review-agent-svc
  namespace: default
spec:
  selector:
    app: mortgage-review-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP